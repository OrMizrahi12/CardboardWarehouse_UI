<Page x:Class="CardboardWarehouse_UI.Pages.Customer.ShopingCart"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CardboardWarehouse_UI.Pages.Customer"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ShopingCart">

    <Grid>
        <DataGrid  x:Name="CartGrid" Margin="388,137,210,52"  IsReadOnly="True" RowHeaderWidth="0" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="X" Binding="{Binding X}" />
                <DataGridTextColumn Header="Y" Binding="{Binding Y}" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" />
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnDeleteFromCart"  HorizontalAlignment="Left" VerticalAlignment="Top" Content="Delete From Cart" Background="DarkRed" Margin="388,112,0,0" Click="btnDeleteFromCart_Click"/>
        <Label x:Name="totalPriceLabel" Content="Total Price: 0" HorizontalAlignment="Left" FontSize="30" Margin="51,137,0,0" VerticalAlignment="Top" />
        <Button x:Name="BtnPay" Content="Pay" HorizontalAlignment="Left" Margin="219,357,0,0" VerticalAlignment="Top" Background="DarkGreen" Click="BtnPay_Click"/>

    </Grid>
</Page>
